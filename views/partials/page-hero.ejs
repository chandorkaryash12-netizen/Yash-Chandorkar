<section class="page-hero">
  <div class="hero-grid-bg" aria-hidden="true"></div>
  <div class="container">
    <% if (typeof eyebrow !== 'undefined' && eyebrow) { %><span class="eyebrow eyebrow-light"><%= eyebrow %></span><% } %>
    <h1><%= heading %></h1>
    <% if (typeof sub !== 'undefined' && sub) { %><p class="lead"><%= sub %></p><% } %>
  </div>
</section>
